#include <stdio.h>
#include "entrees.h"

int main()
{
    int n;
    printf("Combien de décimals voulez-vous lire ? ");
    scanf("%d", &n);
    float 
    for (int i = 0; i < n; i++) {
        lire_decimal()
    }
        /* code */
        return 0;
}

#include <stdio.h>
#include <ctype.h>

int lire_entier(int *entier) {
    char *ligne;
    char c;

    while (test_entier(ligne)) {
        scanf("%m[^\n]%c", &ligne, &c);
    }
    return 0;
}

test_entier()

#ifdef TESTS

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}

#endif